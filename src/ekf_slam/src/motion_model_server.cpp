#include <iostream>
#include <math.h>
#include "ros/ros.h"

// we want to take an input goal with [x, y, theta] and want to output linear_x linear_y and angular_z vel
//bool mm_exe(ekf_slam::goal_pose::Request &req, ekf_slam::goal_pose::Response &res) {

//}


int main(int argc, char **argv)
{
 // ros::init(argc, argv, "motion_model_server");
 // ros::NodeHandle n;

 // ros::ServiceServer service = n.advertiseService("recv_motion_cmd", mm_exe);
 // ros::spin();

  return 0;
}
